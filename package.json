{
  "name": "qrlink",
  "private": true,
  "description": "QR LINK – statická stránka. 'build' skladá content/*.json (edituje sa cez Decap CMS na /admin) do js/data.js.",
  "scripts": {
    "build": "node scripts/build-data.js"
  }
}
